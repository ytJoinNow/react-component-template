'use strict';

const daxueDesignMobile = require('..');
const assert = require('assert').strict;

assert.strictEqual(daxueDesignMobile(), 'Hello from daxueDesignMobile');
console.info('daxueDesignMobile tests passed');
